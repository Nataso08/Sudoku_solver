# Sudoku_solver
A sudoku solver that use the DLX solving algorithm.

## Steps
- Input size
- Input Sudoku
- Recursive solving (with backtracking)

## Use
To compile and use these files, you need to use the command
``` pwsh
g++ main.cpp src/solve.cpp -o main
```